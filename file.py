# open() returns a "file handle"
# -handle = open(filename,mode)
# mode is optional, r-read the file, w-write the file

# each line from the file has a newline at the end
# we can use rstrip() to solve it

fhand = open("mbox.txt") 